const mocha = require('mocha'),
      chai = require('chai'),
      sinon = require('sinon'),
      ProductController = require('../../../services/products-service.js');

let expect = chai.expect;

describe('Products service tests', function() { });