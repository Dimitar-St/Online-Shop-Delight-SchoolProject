mixin message()
    if typeof message !== 'undefined'
        .message.hidden(style='display:none;')
            if message.success
                p.success-message
                    = message.success
            if message.error 
                p.error-message
                    = message.error